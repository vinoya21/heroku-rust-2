// searchbar_rec.js
// Rust Team Project

function searchRec() {
    var searchText = document.getElementById("searchbar_input_rec");
    var searchInputRec = searchText.value;
    alert(searchInputRec);
}
