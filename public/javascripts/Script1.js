// searchbar_artpage.js
// Rust Team Project

function searchArtPage() {
    var searchText = document.getElementById("searchbar_input_art");
    var searchInput = searchText.value;
    alert(searchInput);
}